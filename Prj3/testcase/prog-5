int g(int x, int y) {
  int* a = &x;
  int* b = &y;
  int ret = 0;
  int* ptr = &ret;
  while( *a > 0 ) {
	x = x - 1;
	ret = *ptr + 1;
  }
  if(*b == 0) {
  	return y; 
  }
  else {
  	return ret;
  }
  
}
